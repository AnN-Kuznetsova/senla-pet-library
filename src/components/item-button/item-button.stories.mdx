import { Meta, DocsContainer, Story, Canvas } from '@storybook/addon-docs';
import { ThemeProvider } from '@mui/material/styles';
import { muiTheme } from 'storybook-addon-material-ui5';

import { THEME } from "../../const";
import { ItemButton, ItemButtonMode } from "./item-button";


<Meta
  title="MDX/ItemButton"
  component={ItemButton}
  parameters={{
    docs: {
      container: ({ children, context }) => (
        <DocsContainer context={context}>
          <ThemeProvider theme={THEME}>
            {children}
          </ThemeProvider>
        </DocsContainer>
      ),
    },
  }}
/>

# ItemButton

`ItemButton` - small context button:

<Canvas>
  <ItemButton
    onClick={()=>{}}
    textValue="Ok"
  />
</Canvas>

`ItemButton` - disabled:

<Canvas>
  <ItemButton
    textValue="Ok"
    isDisabled={true}
  />
</Canvas>

`ItemButton` - "more":

<Canvas>
  <ItemButton
    className={ItemButtonMode.MORE}
    onClick={()=>{}}
  />
</Canvas>

`ItemButton` - "warning":

<Canvas>
  <ItemButton
    className={ItemButtonMode.WARNING}
    textValue="Return"
    onClick={()=>{}}
  />
</Canvas>

`ItemButton` - "delete":

<Canvas>
  <ItemButton
    className={ItemButtonMode.DELETE}
    onClick={()=>{}}
  />
</Canvas>

`ItemButton` - "close":

<Canvas>
  <div style={{
    position: "relative",
    top: "-25px",
    width: "100px",
    height: "40px",
  }}>
    <ItemButton
      className={ItemButtonMode.CLOSE}
      onClick={()=>{}}
    />
  </div>
</Canvas>
