/* import {createSlice} from "@reduxjs/toolkit";


const applicationSlice = createSlice({
  name: `application`,
  initialState: {
    isBooksListShow: false,
  },
  reducers: {
    changeBooksListShowed: (state, action) => {state.isBooksListShow = !state.isBooksListShow},
  },
});

const {actions, reducer} = applicationSlice;


export const {
  changeBooksListShowed,
} = actions;

export {
  reducer,
}; */
